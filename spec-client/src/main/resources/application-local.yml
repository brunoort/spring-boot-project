server:
  port: ${PORT:8086}

store:
  url:
    rs-client: ${URL_RS_CLIENT:}
    
spring:
  application:
    name:  ${APP_NAME:spec-client}
    
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL1:http://localhost:8761/eureka/eureka}
  instance:
    prefer-ip-address: true